<resources>

    <!-- Base application theme. -->
    <style name="Theme.Napkin" parent="Theme.AppCompat.Light.DarkActionBar">

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/actionbar</item>
        <item name="colorPrimaryDark">@color/actionbar_dark</item>
        <item name="colorAccent">@color/actionbar</item>
        <item name="android:statusBarColor">@color/actionbar_dark</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="actionBarStyle">@style/Theme.Napkin.ActionBar</item>

    </style>

    <style name="Theme.Napkin.ActionBar" parent="style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="elevation">0dp</item>
        <item name="android:elevation">0dp</item>
    </style>

    <style name="Theme.Napkin.Splash" parent="@style/Theme.Napkin.NoActionBar">
        <item name="android:windowBackground">@color/actionbar</item>
    </style>

    <style name="Theme.Napkin.NoActionBar" parent="@style/Theme.Napkin">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>


    <style name="Theme.Napkin.Translucent" parent="@style/Theme.Napkin.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>
